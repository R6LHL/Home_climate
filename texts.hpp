#ifndef __TEXTS_HPP
#define __TEXTS_HPP

#define RESET_REASON	(F("Reset reason: "))

#define PREHEATING_TEXT	(F("CO2 sensor preheating..."))

#define CO2_TEXT	(F("CO2:"))
#define PPM_TEXT	(F("ppm"))

#define TEMP_TEXT 	(F("t:"))
#define dC_TEXT		(F("dC"))

#define HUM_TEXT 		(F("Hum:"))
#define PRECENT_TEXT	(F("%"))

#define PRS_TEXT	 	(F("Prs:"))
#define KPA_TEXT		(F("kPa"))

#define SPACE_TEXT (F(" "))

#endif //__TEXTS_HPP